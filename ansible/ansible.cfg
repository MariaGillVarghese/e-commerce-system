[defaults]
inventory = production/company/inventory.ini
remote_user = ec2-user
host_key_checking = False
allow_world_readable_tmpfiles = True
# specify private_key_file if needed
#private_key_file = /path/to/key.pem
